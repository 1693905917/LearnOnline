spring:
  application:
    name: content-service #服务名
  profiles:
    active: dev #环境名
  #Nacos配置
  cloud:
    nacos:
      server-addr: 192.168.101.65:8848
      config:  #配置文件相关配置
        namespace: dev
        group: LearnOnline
        file-extension: yaml
        refresh-enabled: true
        shared-configs: #引用公用的配置文件
#          - data-id: swagger-${spring.profiles.active}.yaml
#            group: LearnOnline-common
#            refresh: true
          - data-id: logging-${spring.profiles.active}.yaml
            group: LearnOnline-common
            refresh: true
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://192.168.101.65:3306/ol_content?serverTimezone=UTC&userUnicode=true&useSSL=false&
#    username: root
#    password: mysql
# 日志文件配置路径
#logging:
#  config: classpath:log4j2-dev.xml

